---
import { FeaturedImageSlide } from '@/libs/FeaturedImage';
import SectionBody from '@/libs/SectionBody';
import PrimaryButton from '@/libs/buttons/PrimaryButton';
import Header1 from '@/libs/headers/Header1';
import Header2 from '@/libs/headers/Header2';
import Paragraph from '@/libs/headers/Paragraph';

import type { Image } from '@/libs/stores/init';

type Props = {
  featuredImages: Image[];
};

const { featuredImages } = Astro.props;
---

<section class="mt-32" id="about">
  <Header1 title="ABOUT" titleJa="このイベントについて" className="mb-16" client:load />

  <div class="flex flex-col md:flex-row">
    <div class="w-full md:w-1/2">
      <FeaturedImageSlide images={featuredImages} client:load />
    </div>

    <div class="mt-20 md:mt-10 w-full md:w-1/2">
      <SectionBody client:load>
        <Header2 title="初めてでも楽しめるバーチャルDJイベント" className="mb-10" />

        <Paragraph className="w-full">
          「バーチャルケモナイト」は、VRChatで開催されるオールナイトのDJイベントです！🥳
        </Paragraph>

        <Paragraph className="mt-4 w-full">
          まだVRChatに触れたことがないケモナーの皆さん、DJイベントは初めてという方も大歓迎！✨
          最高の音楽と空間で、きっと忘れられない夜になりますよ。
        </Paragraph>

        <Paragraph className="mt-4 w-full">
          PCはもちろん、Meta Quest単体や、なんとスマホからでも参加OK！📱
          気軽に立ち寄って、私たちと一緒に最高の夜を過ごしませんか？🙌
          最高の音楽を用意して、あなたをお待ちしています！🎧
        </Paragraph>

        <PrimaryButton
          label="参加する"
          href="#timeschedule"
          className="mt-10"
        />
      </SectionBody>
    </div>
  </div>
</section>
